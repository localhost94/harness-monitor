## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## Checklist

- [ ] `cargo test` passes
- [ ] `cargo clippy --all-targets` is clean
- [ ] If a UI change: screenshots from `./scripts/preview.sh` in both themes
      (`?theme=dark`) are attached
- [ ] If a new adapter: it claims no state it cannot verify, and its
      `FidelityTier` is honest about what the harness actually reports

## Anything reviewers should know

<!-- Trade-offs, things you were unsure about, anything you decided not to do. -->
